Open your Firebase Console -> Firestore Database.

Go to the clinicSettings collection.

Find your settings document.

Add a field:

Field name: shutdown

Type: boolean

Value: true

Instantly: Every open app (Admin and Operator) will turn into a dark lock screen. To unlock, simply flip the value to false

remaining:
Patient details tab history should be rendered reverse.
assigned doctor's name should be appeared rather than his id.
once the license system is verified working correctly then Add feature of temporing-preventation.